server:
  port: 1234

spring:
  application:
    name: ISU-SERVICE
  profiles:
    active: development
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      username: admin
      password: admin