server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/semwett0301/soa-labs-config
  security:
    user:
      name: admin
      password: admin